{
  "name": "my-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "npx serve dist -s -l 5000 -H 0.0.0.0",
    "build:prod": "vite build --mode production"
  },
  "dependencies": {
    "serve": "^14.2.4",
    "vite": "^5.4.8"
  }
}
