{
  "name": "portfolio-thiago",
  "version": "1.0.0",
  "description": "Portfólio pessoal de Thiago",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "postbuild": "mkdir -p dist/attached_assets && cp -r attached_assets/* dist/attached_assets/ 2>/dev/null || true && cp script.js dist/ 2>/dev/null || true && cp project-detail.js dist/ 2>/dev/null || true",
    "preview": "vite preview",
    "start": "npx http-server dist -p 80 --no-cache"
  },
  "keywords": ["portfolio", "website", "vite", "html", "css", "javascript"],
  "author": "Thiago",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0",
    "terser": "^5.19.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}