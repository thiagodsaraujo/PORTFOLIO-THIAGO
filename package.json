{
  "name": "my-project",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -r attached_assets dist/",
    "preview": "vite preview",
    "build:prod": "vite build --mode production && cp -r attached_assets dist/",
    "start": "vite preview --port 5000 --host 0.0.0.0"
  },
  "dependencies": {
    "serve": "^14.2.4",
    "vite": "^5.4.8"
  }
}