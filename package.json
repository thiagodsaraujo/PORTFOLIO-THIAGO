{
  "name": "portfolio-thiago",
  "version": "1.0.0",
  "description": "Portfólio pessoal de Thiago",
  "type": "module",
  "scripts": {
    "dev": "vite --force",
    "build": "vite build --force",
    "postbuild": "mkdir -p dist/attached_assets && if [ -d 'attached_assets' ]; then cp -r attached_assets/* dist/attached_assets/; fi && if [ -f 'script.js' ]; then cp script.js dist/; fi && if [ -f 'project-detail.js' ]; then cp project-detail.js dist/; fi",
    "preview": "vite preview",
    "start": "npx http-server dist -p 80 --no-cache"
  },
  "keywords": ["portfolio", "website", "vite", "html", "css", "javascript"],
  "author": "Thiago",
  "license": "MIT",
  "devDependencies": {
    "vite": "^5.0.0",
    "terser": "^5.19.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}